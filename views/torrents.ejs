<% layout('layout') %>

<%- include("modules/header.ejs") %>

<main class="container-xxl py-4">
  <h1 class="h3 mb-3">Торренты</h1>

  <div class="row g-4">
    <!-- Поиск на YTS.mx -->
    <div class="col-12">
      <div class="card rounded-4">
        <div class="card-body">
          <h2 class="h5 mb-3">Поиск фильмов</h2>
          <form id="ytsSearchForm" class="d-grid gap-3">
            <div class="row">
              <div class="col-md-8">
                <input type="text" id="ytsSearchInput" class="form-control rounded-4" placeholder="Введите название фильма для поиска" required>
              </div>
              <div class="col-md-4">
                <button class="btn btn-primary rounded-4 w-100" type="submit">Поиск</button>
              </div>
            </div>
          </form>
          
          <div id="ytsResults" class="mt-3" style="display: none;">
            <h3 class="h6">Результаты поиска:</h3>
            <div id="ytsResultsList" class="d-flex flex-column gap-2"></div>
          </div>
          
          <div id="ytsFiles" class="mt-3" style="display: none;">
            <h3 class="h6">Файлы торрента:</h3>
            <div id="ytsFilesList" class="d-flex flex-column gap-1"></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Добавление торрентов -->
    <div class="col-12 col-lg-4">
      <div class="card rounded-4">
        <div class="card-body d-grid gap-3">
          <h2 class="h5 m-0">Добавить</h2>
          <form id="magnetForm" class="d-grid gap-2">
            <input class="form-control rounded-4" id="magnetInput" placeholder="Вставьте magnet-ссылку"/>
            <button class="btn btn-primary rounded-4" type="submit">Добавить по magnet</button>
          </form>
          <div class="text-center text-muted">или</div>
          <div id="dropZone" class="border rounded-4 p-4 text-center" style="border-style:dashed">
            Перетащите .torrent сюда
            <input id="fileInput" type="file" accept=".torrent" hidden />
            <div class="mt-2">
              <button class="btn btn-outline-secondary btn-sm rounded-4" id="chooseBtn" type="button">Выбрать файл</button>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Список загрузок -->
    <div class="col-12 col-lg-8">
      <div class="card rounded-4">
        <div class="card-body">
          <h2 class="h5">Загрузки</h2>
          <div id="list" class="d-flex flex-column gap-3"></div>
        </div>
      </div>
    </div>
  </div>
  <!-- Модал деталей YTS -->
  <div class="modal fade" id="ytsDetailsModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="ytsDetailsTitle">Детали</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <div id="ytsDetailsBody">Загрузка...</div>
        </div>
      </div>
    </div>
  </div>
</main>

<script src="/socket.io/socket.io.js"></script>
<script>
  const listEl = document.getElementById('list');
  const socket = io();

  const fmtSize = (n) => {
    if (!n && n !== 0) return '';
    const units = ['B','KB','MB','GB','TB'];
    let i=0; let v=n;
    while (v>=1024 && i<units.length-1) { v/=1024; i++; }
    return v.toFixed(1)+' '+units[i];
  };

  function renderItem(t) {
    const id = 't_'+t.infoHash;
    let el = document.getElementById(id);
    
    // Если элемент не существует, создаем его
    if (!el) {
      el = document.createElement('div');
      el.id = id;
      el.className = 'p-3 rounded-4 border d-grid gap-2';
      el.innerHTML = `
        <div class="d-flex justify-content-between align-items-center">
          <div class="fw-medium text-truncate" title="${t.name||t.infoHash}">${t.name||t.infoHash}</div>
          <div class="d-flex gap-2">
            <button class="btn btn-outline-secondary btn-sm rounded-4" data-toggle="${t.paused ? 'resume' : 'pause'}">${t.paused ? 'Пуск' : 'Пауза'}</button>
            <button class="btn btn-outline-warning btn-sm rounded-4" data-force-start="${t.infoHash}" title="Принудительный запуск">⚡</button>
            <button class="btn btn-outline-success btn-sm rounded-4" data-update-trackers="${t.infoHash}" title="Обновить трекеры">🔄</button>
            <button class="btn btn-outline-info btn-sm rounded-4" data-diagnose="${t.infoHash}" title="Диагностика">🔍</button>
            <div class="btn-group">
              <button class="btn btn-outline-danger btn-sm rounded-4" data-remove="${t.infoHash}">Удалить</button>
              <button class="btn btn-outline-danger btn-sm rounded-4 dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false"></button>
              <ul class="dropdown-menu px-1 py-2 rounded-4" data-bs-theme="dark">
                <li><button class="dropdown-item rounded-4" data-remove-files="${t.infoHash}">Удалить с файлами</button></li>
              </ul>
            </div>
          </div>
        </div>
        <div class="progress rounded-4" style="height: 18px;">
          <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width: 0%"></div>
        </div>
        <div class="d-flex justify-content-between small text-muted">
          <div><span data-prog>0</span>% • <span data-down>0</span> / <span data-len>0</span></div>
          <div>⬇ <span data-ds>0</span>/s • ⬆ <span data-us>0</span>/s • peers: <span data-p>0</span></div>
        </div>
        <div class="mt-2 d-flex flex-wrap gap-2" data-files></div>
        <div class="mt-1 small text-muted" data-status></div>
      `;
      
      // Добавляем в список
      listEl.appendChild(el);
      
      // Добавляем обработчики событий
      el.querySelector('[data-remove]').onclick = async () => {
        const r = await fetch(`/torrents/api/${t.infoHash}`,{method:'DELETE'});
        const d = await r.json().catch(()=>({}));
        if (d && d.success) el.remove();
      };
      
      const removeFilesBtn = el.querySelector('[data-remove-files]');
      removeFilesBtn.onclick = async () => {
        if (!confirm('Удалить торрент и все скачанные данные?')) return;
        const r = await fetch(`/torrents/api/${t.infoHash}?deleteFiles=1`,{method:'DELETE'});
        const d = await r.json().catch(()=>({}));
        if (d && d.success) el.remove();
      };
      
      const toggleBtn = el.querySelector('[data-toggle]');
      toggleBtn.onclick = async () => {
        const action = toggleBtn.getAttribute('data-toggle');
        if (action === 'pause') {
          await fetch(`/torrents/api/${t.infoHash}/pause`, { method:'POST' });
        } else {
          await fetch(`/torrents/api/${t.infoHash}/resume`, { method:'POST' });
        }
      };
      
      // Обработчик для кнопки принудительного запуска
      const forceStartBtn = el.querySelector('[data-force-start]');
      forceStartBtn.onclick = async () => {
        try {
          const response = await fetch(`/torrents/api/${t.infoHash}/force-start`, { method:'POST' });
          const result = await response.json();
          if (result.success) {
            console.log('Торрент принудительно запущен');
          } else {
            console.error('Ошибка принудительного запуска:', result.msg);
          }
        } catch (error) {
          console.error('Ошибка сети при принудительном запуске:', error);
        }
      };
      
      // Обработчик для кнопки диагностики
      const diagnoseBtn = el.querySelector('[data-diagnose]');
      diagnoseBtn.onclick = async () => {
        try {
          const response = await fetch(`/torrents/api/${t.infoHash}/diagnose`);
          const result = await response.json();
          if (result.success) {
            console.log('Диагностика торрента:', result.diagnosis);
            // Показываем диагностику в модальном окне или alert
            const diagnosisText = `
Диагностика торрента:
- Название: ${result.diagnosis.name || 'Неизвестно'}
- Прогресс: ${result.diagnosis.progress}%
- Пиры: ${result.diagnosis.numPeers}
- Сиды: ${result.diagnosis.numSeeds}
- Личи: ${result.diagnosis.numLeeches}
- Скорость загрузки: ${result.diagnosis.downloadSpeed || 0} B/s
- Метаданные: ${result.diagnosis.hasMetadata ? 'Загружены' : 'Не загружены'}
- Файлы: ${result.diagnosis.filesCount}
- Выбранные файлы: ${result.diagnosis.selectedFiles}
- Куски: ${result.diagnosis.piecesCount}
- Выбранные куски: ${result.diagnosis.selectedPieces}
- DHT: ${result.diagnosis.dht}
- LPD: ${result.diagnosis.lpd}
- Трекеры: ${result.diagnosis.trackers.length > 0 ? result.diagnosis.trackers.map(t => t.url).join(', ') : 'Нет'}
- Ошибка: ${result.diagnosis.error || 'Нет'}
            `;
            alert(diagnosisText);
          } else {
            console.error('Ошибка диагностики:', result.msg);
          }
        } catch (error) {
          console.error('Ошибка сети при диагностике:', error);
        }
      };
      
      // Обработчик для кнопки обновления трекеров
      const updateTrackersBtn = el.querySelector('[data-update-trackers]');
      updateTrackersBtn.onclick = async () => {
        try {
          const response = await fetch(`/torrents/api/${t.infoHash}/update-trackers`, { method:'POST' });
          const result = await response.json();
          if (result.success) {
            console.log('Трекеры обновлены');
            // Обновляем статус
            const statusEl = el.querySelector('[data-status]');
            if (statusEl) {
              statusEl.textContent = '🔄 Трекеры обновлены...';
              statusEl.className = 'mt-1 small text-info';
            }
          } else {
            console.error('Ошибка обновления трекеров:', result.msg);
            alert('Ошибка обновления трекеров: ' + result.msg);
          }
        } catch (error) {
          console.error('Ошибка сети при обновлении трекеров:', error);
          alert('Ошибка сети при обновлении трекеров');
        }
      };
    }
    
    // Обновляем содержимое элемента
    const bar = el.querySelector('.progress-bar');
    const prog = el.querySelector('[data-prog]');
    const down = el.querySelector('[data-down]');
    const len = el.querySelector('[data-len]');
    const ds = el.querySelector('[data-ds]');
    const us = el.querySelector('[data-us]');
    const p = el.querySelector('[data-p]');
    const files = el.querySelector('[data-files]');
    const toggleBtn = el.querySelector('[data-toggle]');
    const nameEl = el.querySelector('.fw-medium');
    
    // Обновляем название
    if (nameEl) {
      const displayName = t.name || 'Загрузка метаданных...';
      nameEl.textContent = displayName;
      nameEl.title = displayName;
      
      // Добавляем индикатор загрузки метаданных
      if (displayName.includes('Загрузка метаданных')) {
        nameEl.classList.add('text-muted');
        nameEl.classList.add('fst-italic');
      } else {
        nameEl.classList.remove('text-muted');
        nameEl.classList.remove('fst-italic');
      }
    }
    
    // Обновляем кнопку пауза/пуск
    if (toggleBtn) {
      const paused = !!t.paused;
      toggleBtn.textContent = paused ? 'Пуск' : 'Пауза';
      toggleBtn.setAttribute('data-toggle', paused ? 'resume' : 'pause');
    }

    // Обновляем прогресс
    if (bar) {
      const progress = t.progress || 0;
      bar.style.width = progress + "%";
      
      // Добавляем анимацию для загрузки метаданных
      if (progress === 0 && t.name && t.name.includes('Загрузка метаданных')) {
        bar.classList.add('progress-bar-animated');
      } else {
        bar.classList.remove('progress-bar-animated');
      }
    }
    
    if (prog) prog.textContent = t.progress || 0;
    if (down) down.textContent = fmtSize(t.downloaded || 0);
    if (len) len.textContent = fmtSize(t.length || 0);
    if (ds) ds.textContent = fmtSize(t.downloadSpeed || 0);
    if (us) us.textContent = fmtSize(t.uploadSpeed || 0);
    if (p) p.textContent = t.numPeers || 0;

    // Обновляем файлы
    if (files) {
      files.innerHTML = '';
      if (!t.files || !t.files.length) {
        const span = document.createElement('span');
        span.className = 'text-muted small';
        // Проверяем, есть ли метаданные
        if (t.name && t.name.includes('Загрузка метаданных')) {
          span.textContent = 'Файлы не загружены (идёт получение метаданных)';
          span.classList.add('fst-italic');
        } else {
          span.textContent = 'Файлы не найдены';
        }
        files.appendChild(span);
      } else {
        t.files.forEach(f => {
          const playMime = ['video/mp4','video/webm','audio/mpeg','audio/mp3','audio/aac','audio/ogg'];
          const btn = document.createElement('a');
          btn.className = 'btn btn-outline-primary btn-sm rounded-4';
          btn.textContent = f.name;
          const canPlay = playMime.includes(f.mime);
          btn.href = canPlay ? `/torrents/stream/${t.infoHash}/${f.index}` : '#';
          btn.target = canPlay ? '_blank' : '';
          if (!canPlay) btn.classList.add('disabled');
          files.appendChild(btn);
        });
      }
    }
    
    // Обновляем статус торрента
    const statusEl = el.querySelector('[data-status]');
    if (statusEl) {
      let statusText = '';
      let statusClass = 'text-muted';
      
      if (t.paused) {
        statusText = '⏸️ Торрент на паузе';
        statusClass = 'text-warning';
      } else if (t.progress === 0 && t.numPeers === 0) {
        statusText = '🔍 Поиск пиров...';
        statusClass = 'text-info';
      } else if (t.progress === 0 && t.numPeers > 0) {
        statusText = '📥 Подключение к пирам...';
        statusClass = 'text-primary';
      } else if (t.progress > 0 && t.progress < 100) {
        statusText = '📥 Загрузка...';
        statusClass = 'text-success';
      } else if (t.progress === 100) {
        statusText = '✅ Загрузка завершена';
        statusClass = 'text-success';
      }
      
      statusEl.textContent = statusText;
      statusEl.className = `mt-1 small ${statusClass}`;
    }
  }

  // Обработка событий сокетов
  socket.on('torrent:add', (torrent) => {
    console.log('Добавлен новый торрент:', torrent);
    renderItem(torrent);
  });
  
  socket.on('torrent:update', (torrent) => {
    console.log('Обновление торрента:', torrent);
    renderItem(torrent);
  });
  
  socket.on('torrent:remove', ({infoHash}) => {
    console.log('Удален торрент:', infoHash);
    const el = document.getElementById('t_'+infoHash);
    if (el) el.remove();
  });

  // Загружаем существующие торренты при загрузке страницы
  (async () => {
    try {
      const res = await fetch('/torrents/api/list');
      const data = await res.json();
      if (data && data.items) {
        console.log('Загружено торрентов:', data.items.length);
        data.items.forEach(renderItem);
      }
    } catch (error) {
      console.error('Ошибка загрузки списка торрентов:', error);
    }
  })();

  // Периодическое обновление статуса торрентов (каждые 2 секунды)
  setInterval(async () => {
    try {
      const res = await fetch('/torrents/api/list');
      const data = await res.json();
      if (data && data.items) {
        data.items.forEach(renderItem);
        
        // Проверяем "зависшие" торренты (0% прогресса более 30 секунд)
        data.items.forEach(torrent => {
          if (torrent.progress === 0 && torrent.numPeers === 0 && !torrent.paused) {
            // Проверяем, не "завис" ли торрент
            const torrentEl = document.getElementById('t_' + torrent.infoHash);
            if (torrentEl) {
              const statusEl = torrentEl.querySelector('[data-status]');
              if (statusEl && statusEl.textContent.includes('Поиск пиров')) {
                // Если торрент долго ищет пиров, показываем предупреждение
                statusEl.textContent = '⚠️ Долгий поиск пиров - попробуйте принудительный запуск';
                statusEl.className = 'mt-1 small text-warning';
              }
            }
          }
        });
      }
    } catch (error) {
      // Игнорируем ошибки при периодическом обновлении
    }
  }, 2000);
  
  // Автоматическая проверка "зависших" торрентов каждые 30 секунд
  setInterval(async () => {
    try {
      const res = await fetch('/torrents/api/list');
      const data = await res.json();
      if (data && data.items) {
        data.items.forEach(torrent => {
          // Если торрент на 0% более 30 секунд и нет пиров, предлагаем принудительный запуск
          if (torrent.progress === 0 && torrent.numPeers === 0 && !torrent.paused) {
            const torrentEl = document.getElementById('t_' + torrent.infoHash);
            if (torrentEl) {
              const statusEl = torrentEl.querySelector('[data-status]');
              if (statusEl) {
                statusEl.innerHTML = '⚠️ Торрент не прогрессирует - <button class="btn btn-warning btn-sm" onclick="forceStartTorrent(\'' + torrent.infoHash + '\')">Принудительный запуск</button>';
                statusEl.className = 'mt-1 small text-warning';
              }
            }
          }
        });
      }
    } catch (error) {
      // Игнорируем ошибки
    }
  }, 30000);

  document.getElementById('magnetForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const val = document.getElementById('magnetInput').value.trim();
    if (!val) return;
    
    try {
      const response = await fetch('/torrents/api/add-magnet', { 
        method:'POST', 
        headers:{'Content-Type':'application/json'}, 
        body: JSON.stringify({ magnet: val }) 
      });
      
      const result = await response.json();
      if (result.success) {
        console.log('Торрент добавлен:', result.infoHash);
        // Очищаем поле ввода
        document.getElementById('magnetInput').value = '';
      } else {
        console.error('Ошибка добавления торрента:', result.msg);
        alert('Ошибка добавления торрента: ' + result.msg);
      }
    } catch (error) {
      console.error('Ошибка сети при добавлении торрента:', error);
      alert('Ошибка сети при добавлении торрента');
    }
  });

  const dz = document.getElementById('dropZone');
  const fi = document.getElementById('fileInput');
  document.getElementById('chooseBtn').onclick = () => fi.click();
  ['dragenter','dragover','dragleave','drop'].forEach(ev => dz.addEventListener(ev, e => { e.preventDefault(); e.stopPropagation(); }));
  dz.addEventListener('dragover', () => dz.classList.add('bg-body-tertiary'));
  dz.addEventListener('dragleave', () => dz.classList.remove('bg-body-tertiary'));
  dz.addEventListener('drop', async (e) => {
    dz.classList.remove('bg-body-tertiary');
    const file = e.dataTransfer.files[0];
    if (!file) return;
    
    try {
      const fd = new FormData();
      fd.append('torrent', file);
      const response = await fetch('/torrents/api/add-file', { method:'POST', body: fd });
      const result = await response.json();
      
      if (result.success) {
        console.log('Торрент из файла добавлен:', result.infoHash);
      } else {
        console.error('Ошибка добавления торрента из файла:', result.msg);
        alert('Ошибка добавления торрента: ' + result.msg);
      }
    } catch (error) {
      console.error('Ошибка сети при добавлении торрента из файла:', error);
      alert('Ошибка сети при добавлении торрента');
    }
  });
  
  fi.addEventListener('change', async (e) => {
    const file = e.target.files[0];
    if (!file) return;
    
    try {
      const fd = new FormData();
      fd.append('torrent', file);
      const response = await fetch('/torrents/api/add-file', { method:'POST', body: fd });
      const result = await response.json();
      
      if (result.success) {
        console.log('Торрент из файла добавлен:', result.infoHash);
        // Очищаем поле файла
        fi.value = '';
      } else {
        console.error('Ошибка добавления торрента из файла:', result.msg);
        alert('Ошибка добавления торрента: ' + result.msg);
      }
    } catch (error) {
      console.error('Ошибка сети при добавлении торрента из файла:', error);
      alert('Ошибка сети при добавлении торрента');
    }
  });

  // YTS поиск
  const ytsSearchForm = document.getElementById('ytsSearchForm');
  const ytsSearchInput = document.getElementById('ytsSearchInput');
  const ytsResults = document.getElementById('ytsResults');
  const ytsResultsList = document.getElementById('ytsResultsList');
  const ytsFiles = document.getElementById('ytsFiles');
  const ytsFilesList = document.getElementById('ytsFilesList');

  ytsSearchForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const query = ytsSearchInput.value.trim();
    if (!query) return;

    try {
      ytsResultsList.innerHTML = '<div class="text-center"><div class="spinner-border" role="status"></div><div class="mt-2">Поиск...</div></div>';
      ytsResults.style.display = 'block';
      ytsFiles.style.display = 'none';

      const res = await fetch(`/api/yts/search?query=${encodeURIComponent(query)}`);
      const data = await res.json();

      if (data.success && data.items) {
        renderYtsResults(data.items);
      } else {
        ytsResultsList.innerHTML = `<div class="alert alert-warning">${data.msg || 'Ошибка поиска'}</div>`;
      }
    } catch (error) {
      console.error('Kinozal search error:', error);
      kinozalResultsList.innerHTML = '<div class="alert alert-danger">Ошибка сети</div>';
    }
  });

  function renderYtsResults(items) {
    if (!items || items.length === 0) {
      ytsResultsList.innerHTML = '<div class="alert alert-info">Ничего не найдено</div>';
      return;
    }

    ytsResultsList.innerHTML = items.map(item => `
      <div class="card rounded-3 border">
        <div class="card-body p-3">
          <div class="d-flex gap-3 align-items-start">
            <img src="${item.poster || '/images/not-found.png'}" alt="poster" style="width:64px;height:96px;object-fit:cover;border-radius:8px"/>
            <div class="flex-grow-1">
              <div class="d-flex justify-content-between align-items-start">
                <div>
                  <h6 class="mb-1">${item.title}</h6>
                  <div class="small text-muted">
                    Размер: ${item.size || ''} | Сиды: ${item.seeds} | Личи: ${item.leeches} | Рейтинг: ${item.rating ?? ''}
                  </div>
                </div>
                      <div class="d-flex gap-2">
        <button class="btn btn-outline-secondary btn-sm rounded-3" onclick="openYtsDetails('${item.movieId}')">Детали</button>
        ${item.torrents && item.torrents.length > 0 ? `<button class="btn btn-outline-primary btn-sm rounded-3" onclick="showYtsTorrents('${item.movieId}')">Торренты</button>` : ''}
        ${item.torrents && item.torrents.length > 0 ? `<button class="btn btn-outline-success btn-sm rounded-3" onclick="addBestTorrent('${item.movieId}')">Добавить лучший</button>` : ''}
      </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    `).join('');
  }

  async function showYtsTorrents(movieId) {
    try {
      ytsFilesList.innerHTML = '<div class="text-center"><div class="spinner-border spinner-border-sm" role="status"></div><div class="mt-2">Загрузка торрентов...</div></div>';
      ytsFiles.style.display = 'block';

      const res = await fetch(`/api/yts/details/${movieId}`);
      const data = await res.json();

      if (data.success && data.info && data.info.torrents) {
        renderYtsTorrents(data.info.torrents);
      } else {
        ytsFilesList.innerHTML = `<div class="alert alert-warning">Торренты не найдены</div>`;
      }
    } catch (error) {
      console.error('Torrents error:', error);
      ytsFilesList.innerHTML = '<div class="alert alert-danger">Ошибка сети</div>';
    }
  }

  function renderYtsTorrents(torrents) {
    if (!torrents || torrents.length === 0) {
      ytsFilesList.innerHTML = '<div class="alert alert-info">Торренты не найдены</div>';
      return;
    }

    ytsFilesList.innerHTML = torrents.map(torrent => `
      <div class="d-flex justify-content-between align-items-center p-2 bg-body-secondary rounded-2 mb-2">
        <div class="flex-grow-1">
          <div class="small fw-bold">${torrent.title}</div>
          <div class="small text-muted">Размер: ${torrent.size} | Сиды: ${torrent.seeds} | Личи: ${torrent.leeches}</div>
          ${torrent.quality ? `<div class="small text-muted">Качество: ${torrent.quality}</div>` : ''}
        </div>
        <button class="btn btn-outline-success btn-sm" onclick="addTorrentMagnet('${torrent.link}')">
          Добавить
        </button>
      </div>
    `).join('');
  }

  async function openYtsDetails(movieId) {
    try {
      const res = await fetch(`/api/yts/details/${movieId}`);
      const data = await res.json();
      if (!data.success) { alert(data.msg || 'Ошибка'); return; }
      const m = data.info;
      const body = document.getElementById('ytsDetailsBody');
      document.getElementById('ytsDetailsTitle').textContent = `${m.title} (${m.year})`;
      
      // Определяем, является ли это Kinopoisk фильмом
      const isKinopoisk = movieId.startsWith('kinopoisk_');
      
      if (isKinopoisk) {
        // Для Kinopoisk фильмов
        body.innerHTML = `
          <div class="d-flex gap-3">
            <img src="${m.poster || ''}" style="width:160px;height:240px;object-fit:cover;border-radius:10px" onerror="this.style.display='none'"/>
            <div>
              <div class="mb-2"><strong>Рейтинг Кинопоиск:</strong> ${m.ratingKinopoisk ?? 'Нет данных'}</div>
              <div class="mb-2"><strong>Рейтинг IMDb:</strong> ${m.ratingImdb ?? 'Нет данных'}</div>
              <div class="mb-2"><strong>Жанры:</strong> ${(m.genres||[]).join(', ') || 'Не указаны'}</div>
              <div class="mb-2"><strong>Длительность:</strong> ${m.runtime || 'Не указана'} мин</div>
              <div class="mb-2"><strong>Режиссер:</strong> ${m.director || 'Не указан'}</div>
              <div class="mb-2"><strong>Страны:</strong> ${(m.countries||[]).join(', ') || 'Не указаны'}</div>
              <div class="mb-2"><strong>IMDb ID:</strong> ${m.imdbCode || 'Не указан'}</div>
              <div class="mb-2"><strong>Возрастной рейтинг:</strong> ${m.ageRating ? m.ageRating + '+' : 'Не указан'}</div>
              <div class="mb-2"><strong>Тип:</strong> ${m.type === 'series' ? 'Сериал' : 'Фильм'}</div>
            </div>
          </div>
          <div class="mt-3">${m.overview || m.description || 'Описание недоступно'}</div>
          <h6 class="mt-3">Актеры</h6>
          <div class="d-flex flex-wrap gap-2">
            ${(m.cast||[]).slice(0,12).map(c=>`<span class="badge text-bg-secondary">${c}</span>`).join('') || '<span class="text-muted">Актеры не указаны</span>'}
          </div>
          ${m.torrents && m.torrents.length > 0 ? `
            <h6 class="mt-3">Найденные торренты (${m.torrents.length})</h6>
            <div class="d-flex flex-wrap gap-2">
              ${m.torrents.slice(0, 5).map(t=>`<button class="btn btn-outline-primary btn-sm rounded-3" onclick="addTorrentMagnet('${t.link}')">${t.title.substring(0, 30)}... • ${t.size} • ${t.seeds}↑</button>`).join('')}
            </div>
          ` : `
            <div class="mt-3 alert alert-info">
              <strong>Информация:</strong> Данные получены через официальный Kinopoisk API. Торренты не найдены.
            </div>
          `}
        `;
      } else {
        // Для обычных фильмов (если будут)
        body.innerHTML = `
          <div class="d-flex gap-3">
            <img src="${m.poster || ''}" style="width:160px;height:240px;object-fit:cover;border-radius:10px"/>
            <div>
              <div class="mb-2"><strong>Рейтинг:</strong> ${m.rating ?? ''}</div>
              <div class="mb-2"><strong>Жанры:</strong> ${(m.genres||[]).join(', ')}</div>
              <div class="mb-2"><strong>Длительность:</strong> ${m.runtime || ''} мин</div>
              <div class="mb-2"><strong>IMDB:</strong> ${m.imdbCode || ''}</div>
            </div>
          </div>
          <div class="mt-3">${m.description || ''}</div>
          <h6 class="mt-3">Актеры</h6>
          <div class="d-flex flex-wrap gap-2">
            ${(m.cast||[]).slice(0,12).map(c=>`<span class="badge text-bg-secondary">${c.name}${c.character?` — ${c.character}`:''}</span>`).join('')}
          </div>
          <h6 class="mt-3">Доступные торренты</h6>
          <div class="d-flex flex-wrap gap-2">
            ${m.torrents.map(t=>`<button class="btn btn-outline-primary btn-sm rounded-3" onclick="addYtsMagnet('${t.hash}')">${t.quality} ${t.type} • ${t.size}</button>`).join('')}
          </div>
        `;
      }
      
      const modal = new bootstrap.Modal(document.getElementById('ytsDetailsModal'));
      modal.show();
    } catch (e) {
      alert('Ошибка загрузки деталей');
    }
  }

  async function addTorrentMagnet(magnetLink) {
    try {
      const response = await fetch('/torrents/api/add-magnet', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ magnet: magnetLink })
      });
      
      const result = await response.json();
      if (result.success) {
        console.log('YTS торрент добавлен:', result.infoHash);
        alert('Торрент добавлен в загрузки!');
      } else {
        console.error('Ошибка добавления YTS торрента:', result.msg);
        alert('Ошибка добавления торрента: ' + result.msg);
      }
    } catch (error) {
      console.error('Add torrent error:', error);
      alert('Ошибка добавления торрента');
    }
  }

  async function addBestTorrent(movieId) {
    try {
      const res = await fetch(`/api/yts/details/${movieId}`);
      const data = await res.json();

      if (data.success && data.info && data.info.torrents && data.info.torrents.length > 0) {
        const bestTorrent = data.info.torrents[0]; // Первый торрент (сортированный по сидам)
        const response = await fetch('/torrents/api/add-magnet', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ magnet: bestTorrent.link })
        });
        
        const result = await response.json();
        if (result.success) {
          console.log('Лучший торрент добавлен:', result.infoHash);
          alert(`Добавлен лучший торрент: ${bestTorrent.title.substring(0, 50)}...`);
        } else {
          console.error('Ошибка добавления лучшего торрента:', result.msg);
          alert('Ошибка добавления торрента: ' + result.msg);
        }
      } else {
        alert('Торренты не найдены');
      }
    } catch (error) {
      console.error('Add best torrent error:', error);
      alert('Ошибка добавления торрента');
    }
  }

  async function addYtsMagnet(hash) {
    try {
      // Для YTS нужно создать magnet ссылку из hash
      const magnetLink = `magnet:?xt=urn:btih:${hash}`;
      const response = await fetch('/torrents/api/add-magnet', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ magnet: magnetLink })
      });
      
      const result = await response.json();
      if (result.success) {
        console.log('YTS торрент добавлен:', result.infoHash);
        alert('Торрент добавлен в загрузки!');
        // Закрываем модальное окно
        const modal = bootstrap.Modal.getInstance(document.getElementById('ytsDetailsModal'));
        if (modal) modal.hide();
      } else {
        console.error('Ошибка добавления YTS торрента:', result.msg);
        alert('Ошибка добавления торрента: ' + result.msg);
      }
    } catch (error) {
      console.error('Add YTS torrent error:', error);
      alert('Ошибка добавления торрента');
    }
  }

  async function forceStartTorrent(infoHash) {
    try {
      const response = await fetch(`/torrents/api/${infoHash}/force-start`, { method:'POST' });
      const result = await response.json();
      if (result.success) {
        console.log('Торрент принудительно запущен');
        // Обновляем статус
        const torrentEl = document.getElementById('t_' + infoHash);
        if (torrentEl) {
          const statusEl = torrentEl.querySelector('[data-status]');
          if (statusEl) {
            statusEl.textContent = '⚡ Принудительно запущен...';
            statusEl.className = 'mt-1 small text-info';
          }
        }
      } else {
        console.error('Ошибка принудительного запуска:', result.msg);
        alert('Ошибка принудительного запуска: ' + result.msg);
      }
    } catch (error) {
      console.error('Ошибка сети при принудительном запуске:', error);
      alert('Ошибка сети при принудительном запуске');
    }
  }
</script>